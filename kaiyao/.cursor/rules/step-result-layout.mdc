---
description: 成器页（StepResult）版式与文案展示约定，非用户明确要求不得改动
globs: "**/components/steps/StepResult.tsx"
alwaysApply: false
---

# 成器页版式（冻结）

编辑 `StepResult.tsx` 或成器相关展示时，**除非用户明确要求改版式/结构**，否则须保持当前实现不变。

## 布局与结构

- 整体：左侧器物 + 底部三按钮；右侧 `作品铭牌` → 标题 → 介绍 → `窑变关键词` → `生成参数` 四宫格 → `生成依据` 三列列表；栅格与区块顺序勿随意调整。
- **作品铭牌下介绍**：两段文字放在**同一个** `<p>` 内，中间**仅一个** `<br />` 换行，**不要**双换行（不要空行）。

## 文案展示规则

- 页面上所有由 `artwork` 驱动的中文展示句，经 **`noPeriod()`** 去掉 **`。`**（全角句号）；新增字段展示时同样包一层 `noPeriod`。
- **生成依据**：保留小标题「心境影响」「火候影响」「釉面影响」；正文**不要**加前缀「心境：」「火候：」「釉面：」——只输出 `rationale` 正文。

## 关联文件

- 器物图与滤镜：`ResultVasePreview.tsx`；**成器页与开窑页**须共用 `generateArtwork.ts` 导出的 **`resolveVaseImageSrc(mood, fire, glaze)`** 作为 `imageSrc`，保证两页器物图一致；新增专用成片时只在该函数与预设分支中扩展。
- 成器数据与预设文案：`src/lib/generateArtwork.ts`；改字段时须同步 `StepResult` 绑定，且遵守上文展示规则。
- 成器「专用图 + 固定文案」预设（如静+微火+玉釉、静+中火+玉釉、静+武火+玉釉、静+武火+冰裂釉、静+武火+流釉、静+中火+冰裂釉、静+中火+流釉、静+微火+冰裂釉、静+微火+流釉、敛+微火+玉釉、敛+微火+冰裂釉、敛+微火+流釉、敛+中火+玉釉、敛+中火+冰裂釉、敛+中火+流釉、敛+武火+玉釉、敛+武火+冰裂釉、敛+武火+流釉、望+微火+玉釉、望+微火+冰裂釉、望+微火+流釉、望+中火+玉釉、望+中火+流釉、望+中火+冰裂釉、望+武火+玉釉、望+武火+冰裂釉、望+武火+流釉、惑+微火+玉釉、惑+微火+冰裂釉、惑+微火+流釉、惑+中火+玉釉、惑+中火+流釉、惑+中火+冰裂釉、惑+武火+玉釉、惑+武火+冰裂釉、惑+武火+流釉、烈+微火+玉釉、烈+微火+冰裂釉、烈+微火+流釉、烈+中火+玉釉、烈+中火+冰裂釉、烈+中火+流釉、烈+武火+玉釉、烈+武火+冰裂釉、烈+武火+流釉、流+微火+玉釉、流+微火+冰裂釉、流+微火+流釉、流+中火+玉釉、流+中火+冰裂釉、流+中火+流釉、流+武火+玉釉、流+武火+冰裂釉、流+武火+流釉等）在 `generateArtwork.ts` 分支与 **`resolveVaseImageSrc`** 中同步维护；新增时在 `public/` 放入成片，且预设内 `introParagraphs` 仍须为两段、无句末 `。`（展示层会 `noPeriod`）。
